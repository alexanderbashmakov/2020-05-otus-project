spring:
  application:
    name: eureka-discovery-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:${EUREKA_DISCOVERY_PORT:7002}/eureka
server:
  port: ${EUREKA_DISCOVERY_PORT:7002}


